# Google Cloud Platform (GCP) API Credentials
# ------------------------------------------------
# 1. GOOGLE_APPLICATION_CREDENTIALS는 OAuth 클라이언트 JSON 파일 경로입니다.
# 2. SPREADSHEET_ID는 미리 만들어 둔 Google Sheet ID입니다. (자동 생성 시 비워 두어도 됩니다.)
# 3. RECIPIENT_EMAILS는 결과를 전달할 이메일 목록으로 JSON 배열 또는 쉼표 구분 문자열을 지원합니다.
# 4. (선택) REFERENCE_DATE는 YYYY-MM-DD 형식으로 테스트용 기준 날짜를 강제 지정합니다.

GOOGLE_APPLICATION_CREDENTIALS="/absolute/path/to/credentials.json"
SPREADSHEET_ID=""
RECIPIENT_EMAILS=["user1@example.com", "user2@example.com"]
REFERENCE_DATE=""
